<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
  <div class="max-w-6xl mx-auto p-6">
    <div class="flex items-center justify-between gap-4">
      <div>
        <h1 class="text-2xl font-semibold">GTD_Neto</h1>
        <p class="text-sm text-slate-600">Dashboard general + flujo Collect -> Destino</p>
      </div>
      <nav class="text-sm flex flex-wrap gap-2 items-center">
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/">Dashboard</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/collect">Collect</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/hacer">Hacer</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/agendar">Agendar</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/delegar">Delegar</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/desglosar">Desglosar</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/no-hacer">No hacer</a>
        <a class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100" href="/terminado">Terminado</a>
        <% if (useSupabase && authUser) { %>
          <span class="text-xs text-slate-500"><%= authUser.email %></span>
          <form method="POST" action="/auth/logout">
            <button class="px-3 py-1 rounded-lg bg-white border hover:bg-slate-100">Salir</button>
          </form>
        <% } %>
      </nav>
    </div>

    <% if (flash?.error) { %>
      <div class="mt-4 p-3 rounded-lg bg-red-50 text-red-800 text-sm border border-red-200"><%= flash.error %></div>
    <% } %>

    <main class="mt-6">
      <%- body %>
    </main>
  </div>
</body>
</html>
