<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="Aplicación GTD simplificada para gestión de productividad" />
  <meta name="theme-color" content="#2563eb" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/public/css/styles.css" />
  <link rel="manifest" href="/public/manifest.json" />
  <link rel="apple-touch-icon" href="/public/icon-192.png" />
  <script nonce="<%= cspNonce %>" src="/public/js/theme.js"></script>
</head>
<body class="bg-slate-50 dark:bg-slate-900 text-slate-900 dark:text-slate-100 transition-colors duration-200">
  <a href="#main-content" class="sr-only focus:not-sr-only focus:fixed focus:top-2 focus:left-2 focus:z-50 focus:bg-white focus:text-slate-900 focus:px-3 focus:py-2 focus:rounded">
    Saltar al contenido principal
  </a>
  <div class="max-w-6xl mx-auto p-4 md:p-6">
    <div class="flex flex-col gap-3">
      <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-3">
        <div class="flex items-center gap-3">
          <div>
            <h1 class="text-xl md:text-2xl font-semibold dark:text-slate-100">GTD_Neto</h1>
            <p class="text-xs md:text-sm text-slate-600 dark:text-slate-400">Dashboard general + flujo Collect -> Destino</p>
          </div>
        </div>

        <% if (!hideAppNav && useSupabase && authUser) { %>
          <div class="flex items-center gap-2 rounded-xl border dark:border-slate-700 bg-white/70 dark:bg-slate-900/30 p-1.5 self-start md:self-auto">
            <span class="px-2 py-1 rounded-lg bg-slate-50 dark:bg-slate-800 border dark:border-slate-700 text-xs text-slate-600 dark:text-slate-300">👤 <%= authUser.email %></span>
            <form method="POST" action="/auth/logout">
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button class="px-2 py-1 md:px-3 rounded-lg bg-amber-50 text-amber-800 border border-amber-200 hover:bg-amber-100 dark:bg-amber-900/30 dark:text-amber-200 dark:border-amber-700 transition-colors">🚪 Salir</button>
            </form>
          </div>
        <% } %>
      </div>

      <% if (!hideAppNav) { %>
        <nav class="text-xs md:text-sm" aria-label="Navegación principal">
          <div class="grid grid-cols-1 md:grid-cols-12 gap-2">
            <div class="rounded-xl border dark:border-slate-700 bg-white/70 dark:bg-slate-900/30 p-1.5 md:col-span-3">
              <div class="px-2 pb-1 text-[11px] text-slate-500 dark:text-slate-400">General</div>
              <div class="flex flex-wrap items-center gap-1.5">
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors font-medium" href="/">🧭 Dashboard</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/stats" title="Estadísticas">📈 Estadísticas</a>
              </div>
            </div>

            <div class="rounded-xl border dark:border-slate-700 bg-white/70 dark:bg-slate-900/30 p-1.5 md:col-span-6">
              <div class="px-2 pb-1 text-[11px] text-slate-500 dark:text-slate-400">Ejecución</div>
              <div class="flex flex-wrap items-center gap-1.5">
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/collect">📥 Collect</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/hacer">✅ Hacer</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/agendar">🗓️ Agendar</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/delegar">🤝 Delegar</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/desglosar">🧩 Desglosar</a>
              </div>
            </div>

            <div class="rounded-xl border dark:border-slate-700 bg-white/70 dark:bg-slate-900/30 p-1.5 md:col-span-3">
              <div class="px-2 pb-1 text-[11px] text-slate-500 dark:text-slate-400">Cierre</div>
              <div class="flex flex-wrap items-center gap-1.5">
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/no-hacer">🚫 No hacer</a>
                <a class="px-2 py-1 md:px-3 rounded-lg bg-white dark:bg-slate-800 border dark:border-slate-700 hover:bg-slate-100 dark:hover:bg-slate-700 transition-colors" href="/terminado">🎯 Terminado</a>
              </div>
            </div>
          </div>
        </nav>
      <% } %>
    </div>

    <% if (flash?.error) { %>
      <div class="mt-4 p-3 rounded-lg bg-red-50 dark:bg-red-900/30 text-red-800 dark:text-red-200 text-sm border border-red-200 dark:border-red-800"><%= flash.error %></div>
    <% } %>

    <main id="main-content" class="mt-6" tabindex="-1">
      <%- body %>
    </main>
  </div>

  <!-- Scripts comunes -->
  <script nonce="<%= cspNonce %>" src="/public/js/toast.js"></script>
  <script nonce="<%= cspNonce %>" src="/public/js/utils.js"></script>
  <script nonce="<%= cspNonce %>" src="/public/js/tags.js"></script>

  <!-- PWA Service Worker -->
  <script nonce="<%= cspNonce %>">
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js').catch(err => {
          console.log('SW registration failed:', err);
        });
      });
    }

  </script>

  <!-- Flash message to toast conversion -->
  <% if (flash?.error) { %>
    <script nonce="<%= cspNonce %>">
      document.addEventListener('DOMContentLoaded', () => {
        toast.error(<%- JSON.stringify(String(flash.error || '')) %>);
      });
    </script>
  <% } %>
  <% if (flash?.success) { %>
    <script nonce="<%= cspNonce %>">
      document.addEventListener('DOMContentLoaded', () => {
        toast.success(<%- JSON.stringify(String(flash.success || '')) %>);
      });
    </script>
  <% } %>
</body>
</html>
