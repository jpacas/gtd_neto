<h2 class="text-lg font-semibold">Hacer</h2>
<p class="text-sm text-slate-600 mt-1">Aquí van acciones claras de &lt; 10 minutos. Prioriza por urgencia e importancia.</p>

<div class="mt-4 bg-white border rounded-xl p-4">
  <form method="POST" action="/hacer/add" class="grid grid-cols-1 md:grid-cols-6 gap-2 items-end">
    <div class="md:col-span-3">
      <label class="text-xs text-slate-500">Nueva acción (&lt;10 min)</label>
      <input type="text" name="input" class="w-full border rounded-lg px-2 py-1.5 text-sm" placeholder="Ej: Enviar correo de seguimiento a cliente X" />
    </div>
    <div>
      <label class="text-xs text-slate-500">Urgencia</label>
      <select name="urgency" class="w-full border rounded-lg px-2 py-1.5 text-sm">
        <option value="5">5</option><option value="4">4</option><option value="3" selected>3</option><option value="2">2</option><option value="1">1</option>
      </select>
    </div>
    <div>
      <label class="text-xs text-slate-500">Importancia</label>
      <select name="importance" class="w-full border rounded-lg px-2 py-1.5 text-sm">
        <option value="5">5</option><option value="4">4</option><option value="3" selected>3</option><option value="2">2</option><option value="1">1</option>
      </select>
    </div>
    <div>
      <% if (needApiKey) { %><input type="hidden" name="apiKey" value="<%= apiKey || '' %>" /><% } %>
      <button class="w-full text-sm px-3 py-1.5 rounded-lg bg-blue-600 text-white hover:bg-blue-700">Agregar</button>
    </div>
  </form>
</div>

<div class="mt-4 space-y-2">
  <% if (!items.length) { %>
    <div class="text-sm text-slate-600">No hay acciones en Hacer.</div>
  <% } %>

  <% for (const it of items) { %>
    <div class="bg-white border rounded-lg p-3">
      <form method="POST" action="/hacer/<%= it.id %>/update" class="grid grid-cols-1 md:grid-cols-12 gap-2 items-end">
        <% if (needApiKey) { %><input type="hidden" name="apiKey" value="<%= apiKey || '' %>" /><% } %>
        <div class="md:col-span-6">
          <label class="text-xs text-slate-500">Acción</label>
          <input type="text" name="title" value="<%= it.title || it.input %>" class="w-full border rounded-lg px-2 py-1.5 text-sm" />
        </div>
        <div>
          <label class="text-xs text-slate-500">Urg.</label>
          <input type="number" min="1" max="5" name="urgency" value="<%= it.urgency || 3 %>" class="w-full border rounded-lg px-2 py-1.5 text-sm" />
        </div>
        <div>
          <label class="text-xs text-slate-500">Imp.</label>
          <input type="number" min="1" max="5" name="importance" value="<%= it.importance || 3 %>" class="w-full border rounded-lg px-2 py-1.5 text-sm" />
        </div>
        <div>
          <label class="text-xs text-slate-500">Min</label>
          <input type="number" min="1" max="10" name="estimateMin" value="<%= it.estimateMin || 10 %>" class="w-full border rounded-lg px-2 py-1.5 text-sm" />
        </div>
        <div>
          <label class="text-xs text-slate-500">Prioridad</label>
          <div class="px-2 py-1.5 text-sm rounded-lg border bg-slate-50"><%= it.priorityScore || '-' %></div>
        </div>
        <div>
          <button class="w-full text-sm px-3 py-1.5 rounded-lg bg-blue-600 text-white hover:bg-blue-700">Guardar</button>
        </div>
      </form>

      <div class="mt-2 text-xs">
        <% if (it.actionableOk) { %>
          <span class="px-2 py-0.5 rounded bg-green-50 text-green-700 border border-green-200">Accionable ✓</span>
        <% } else { %>
          <span class="px-2 py-0.5 rounded bg-amber-50 text-amber-800 border border-amber-200">Mejorar redacción</span>
          <span class="text-slate-600 ml-2"><%= it.actionableFeedback || '' %></span>
        <% } %>
      </div>
    </div>
  <% } %>
</div>
