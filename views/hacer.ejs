<div class="flex items-start justify-between gap-3">
  <div>
    <h2 class="text-lg font-semibold">Hacer</h2>
    <p class="text-sm text-slate-600 mt-1">Acciones rápidas (&lt;10 min), ordenadas por prioridad.</p>
  </div>
  <a href="/collect" class="text-sm px-3 py-1.5 rounded-lg border bg-white hover:bg-slate-50">+ Enviar desde Collect</a>
</div>

<div class="mt-4 bg-white border rounded-xl p-4">
  <div class="text-xs text-slate-500">Regla de esta sección</div>
  <div class="text-sm text-slate-700 mt-1">Las actividades de <strong>Hacer</strong> solo se agregan desde <strong>Collect</strong>. Aquí solo se priorizan y afinan.</div>
</div>

<div class="mt-4 space-y-2">
  <% if (!items.length) { %>
    <div class="bg-white border rounded-xl p-5 text-sm text-slate-600">
      No hay acciones en Hacer.
      <a href="/collect" class="underline ml-1">Ir a Collect</a>
    </div>
  <% } %>

  <% for (const it of items) { %>
    <div class="bg-white border rounded-xl p-3">
      <form method="POST" action="/hacer/<%= it.id %>/update" class="grid grid-cols-1 md:grid-cols-12 gap-2 items-center">
        <% if (needApiKey) { %><input type="hidden" name="apiKey" value="<%= apiKey || '' %>" /><% } %>

        <div class="md:col-span-6">
          <input type="text" name="title" value="<%= it.title || it.input %>" class="w-full border rounded-lg px-2.5 py-2 text-sm" />
        </div>

        <div class="md:col-span-1">
          <input type="number" min="1" max="5" name="urgency" value="<%= it.urgency || 3 %>" class="w-full border rounded-lg px-2 py-2 text-sm" title="Urgencia" />
        </div>

        <div class="md:col-span-1">
          <input type="number" min="1" max="5" name="importance" value="<%= it.importance || 3 %>" class="w-full border rounded-lg px-2 py-2 text-sm" title="Importancia" />
        </div>

        <div class="md:col-span-1">
          <input type="number" min="1" max="10" name="estimateMin" value="<%= it.estimateMin || 10 %>" class="w-full border rounded-lg px-2 py-2 text-sm" title="Minutos" />
        </div>

        <div class="md:col-span-1 text-center">
          <span class="inline-flex min-w-10 justify-center px-2 py-1 rounded-lg border bg-slate-50 text-sm"><%= it.priorityScore || '-' %></span>
        </div>

        <div class="md:col-span-2 md:text-right">
          <button class="w-full md:w-auto text-sm px-3 py-2 rounded-lg bg-blue-600 text-white hover:bg-blue-700">Guardar</button>
        </div>
      </form>

      <div class="mt-2 flex flex-wrap items-center gap-2 text-xs">
        <span class="px-2 py-0.5 rounded border bg-slate-50 text-slate-600">Urg <%= it.urgency || 3 %></span>
        <span class="px-2 py-0.5 rounded border bg-slate-50 text-slate-600">Imp <%= it.importance || 3 %></span>
        <span class="px-2 py-0.5 rounded border bg-slate-50 text-slate-600"><%= it.estimateMin || 10 %> min</span>

        <% if (it.actionableOk) { %>
          <span class="px-2 py-0.5 rounded bg-green-50 text-green-700 border border-green-200">Accionable ✓</span>
        <% } else { %>
          <span class="px-2 py-0.5 rounded bg-amber-50 text-amber-800 border border-amber-200">Mejorar redacción</span>
          <span class="text-slate-600"><%= it.actionableFeedback || '' %></span>
        <% } %>
      </div>
    </div>
  <% } %>
</div>
